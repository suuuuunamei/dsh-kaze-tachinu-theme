# Publishing this package must talk to the official registry.
# (The user-level ~/.npmrc points at a read-only mirror for install speed;
# project-level config takes precedence inside this repo.)
registry=https://registry.npmjs.org/
